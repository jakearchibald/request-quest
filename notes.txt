TODOS:
  * Why does compression fail?
  * Upgrade to indexdb
  * Make progressive enhancing version?
  * Code highlighting
  * Push-state urls? (but how to resolve them?)
  * Crueler code when the request has gone

QUESTION IDEAS:
* Try adjacent selectors, Bev thinks adding it is enough

* manifest (dynamically added)

* That CSS example from Andy Hume

* That link ping thing & prefetch

* changing the src of a script element shouldn't request the new file (if the script isn't parser inserted)

* give a ul a bullet image, but no lis

* content property on normal elements?
* ::before image url on img element (or input element)

* Fancy shit with audio/video element?

* SVG sprites

* Media query css

* event source